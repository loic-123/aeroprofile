__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
dist/
build/
node_modules/
frontend/dist/
.DS_Store
*.log
*.log.*
logs/

# Activity files at root (only keep those in tests/fixtures/)
/*.fit
/*.gpx
/*.tcx
/*.zip

# Bulk downloaded activity files (too large for git)
tests/fixtures/laurette/

# Marketing drafts (posts, emails, launch kit) — local only
marketing/

# Claude Code scratch (plans, session notes, memory) — local only
.claude/
